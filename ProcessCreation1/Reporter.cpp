#include "Reporter.h"

namespace reporter {

DWORD __stdcall reporter::Reporter(LPVOID iReporterProps) { return 0; }

Employee* reporter::readEmployeeFromBinary(std::ifstream& in) {
  return nullptr;
}

void reporter::writeReportLine(Employee* employee, double wage) {}

}  // namespace reporter
